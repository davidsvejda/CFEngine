bundle agent ssh
{
  vars:
    debian::
    "pkg[openssh-server]	=> string "*";

    centos_6::
    "pkg[openssh-clients]	=> string "*";

  methods:
    "Call packages bundle and send the array of packages and their versions to be installed" usebundle => packages("ssh.pkg");
}
